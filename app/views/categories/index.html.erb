<h1><%= @categories.length %> Categories</h1>
<% @categories.each do |category|%>
  <ul>
    <li>
      <%= link_to category_products_path(category) do %>
      <%#= category.title %>
      <%= category.title %>(<%= category.products_count %> products)
      <% end %>
    </li>
  </ul>
<% end %>
